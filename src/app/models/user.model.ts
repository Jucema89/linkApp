export class User {
    public name: string;
    public email: string;
    public password: string;
    public avatar: string;

    constructor( texto: string) {
        this.name = texto,
        this.email = texto;
        this.password = texto;
        this.avatar = texto
    }
}